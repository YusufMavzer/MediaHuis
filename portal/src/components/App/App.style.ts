import styled from "styled-components";

const Root = styled.div`
  width: 100vw;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
`;

const S = { Root };

export default S;
